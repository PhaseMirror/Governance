[package]
name = "adr_rust_kani"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
kani = { git = "https://github.com/model-checking/kani", rev = "master" }

[workspace]
# Empty workspace to avoid inheriting parent workspace settings
