[package]
name = "rust-adr-verifier"
version = "0.1.0"
edition = "2021"

[dependencies]

[dev-dependencies]
kani-verifier = "0.48.0" # Typical Kani version, this allows pulling in the kani macros if needed (if building normally). But we can just use #[cfg(kani)] without a dependency sometimes. Let's add it just in case.
